function printMyName() {
    console.log('yori')
}

setTimeout(printMyName,1000) // settimeout делает делей между вызовом функции принтмайнейм в 1000 милисек

// колбэк функция = это фукнция, которая вызывает другую функцию