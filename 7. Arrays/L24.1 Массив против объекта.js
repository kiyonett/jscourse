const myObject = {
    0:1,
    1:2,
    2:3,
    length:3
}

console.log(myObject)

// =============================

const myArray = new Array(1,2,3)

console.log(myArray)

// Отличие заключается в том, что прототипы разные, и соответственно доступные функции тоже разные.
// Ах, и length в массиве автоматически изменяется