const myArray = [1,true, 'popa']
console.log('Оригинальный вывод myArray =',myArray, 'Вывод первого значения(0) =',
    myArray[0], ' ',
    myArray[1], ' ',
    myArray.length
)

// Порядок элементов (индекс) в массиве важен